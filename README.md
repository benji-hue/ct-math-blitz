# ЦТ / ЦЭ Математика — Блиц по формулам и ловушкам РИКЗ

Интерактивный тренажёр для подготовки к централизованному тестированию по математике
(Беларусь). Скоростные раунды по формулам, графикам и типовым ловушкам из вариантов РИКЗ.

```bash
npm install
npm run dev
```

`npm run build` — продакшен-сборка, `npm run lint` — проверка кода.

## Что внутри

**119 микрозаданий** по шести разделам спецификации ЦТ:

| Раздел | Заданий | Что проверяется |
| --- | --- | --- |
| `trigonometry` | 24 | двойной угол, формулы сложения, приведение, понижение степени, табличные значения |
| `stereometry` | 19 | объёмы и поверхности конуса, цилиндра, пирамиды, шара, усечённого конуса |
| `planimetry` | 18 | теоремы синусов и косинусов, площади через $r$ и $R$, медианы, окружность |
| `functions_graphs` | 22 | знаки $k$ и $b$, вершина параболы, асимптоты, преобразования $f(x+a)+b$, производная |
| `powers_and_logs` | 19 | свойства логарифмов, переход к новому основанию, степени, ОДЗ |
| `progressions` | 17 | $n$-й член и суммы арифметической и геометрической прогрессий |

Каждое задание несёт не только правильный ответ, но и **разбор ловушки**: какой именно
дистрактор подкладывают составители и почему на него попадаются.

## Механики

- **Скоростной блиц** — 10/12/15 вопросов, 15–20 секунд на вопрос (зависит от типа),
  комбо-множитель до ×3 за серию, очки зависят от остатка времени.
- **Активное восстановление** — при ошибке таймер замирает, открывается оверлей
  «Попались в ловушку» с правилом, разбором выбранного варианта и мнемоникой.
  Двигаться дальше можно только выбрав верный ответ заново.
- **Банк ошибок** — всё, на чём споткнулись, переспрашивается в конце раунда
  с заново перемешанными вариантами, а также копится между сессиями
  (кнопка «Работа над ошибками» на главном экране).
- **Формульник** — справочник, который открывается по мере игры: формула
  разблокируется после первой встречи и получает статус «Освоено» после двух
  чистых ответов с первой попытки.
- Прогресс хранится в `localStorage` (все обращения обёрнуты в `try/catch`,
  так что приватный режим и заблокированное хранилище не ломают приложение).

## Стек

- Vite + React 19 + TypeScript (strict)
- Tailwind CSS v4 (`@tailwindcss/vite`)
- KaTeX — рендеринг всех формул
- lucide-react — иконки
- Графики — собственный SVG-рендерер без сторонних библиотек

Мобильный-first: вёрстка рассчитана на телефон, учитывает `env(safe-area-inset-bottom)`
а SDK Telegram WebApp подгружается лениво и только если страница реально открыта
внутри Telegram — в нативной сборке и офлайн внешних запросов нет вообще.

## Данные

`src/data/ct_questions.json` — единый массив заданий. Схема одного элемента:

```jsonc
{
  "id": "trig_sin_double",
  "category": "trigonometry",            // одна из шести категорий
  "type": "formula_choice",              // formula_choice | graph_id | true_false
  "question": "Чему равен $\\sin 2\\alpha$?",
  "options": ["$2\\sin\\alpha\\cos\\alpha$", "..."],
  "correct_index": 0,                    // варианты перемешиваются в рантайме
  "graph_spec": null,                    // или { type, params } — см. ниже
  "learning": {
    "rule": "$\\sin 2\\alpha = 2\\sin\\alpha\\cos\\alpha$",
    "common_trap": "Какой дистрактор подкладывает РИКЗ и почему",
    "mnemonic": "Короткая подсказка для запоминания"
  }
}
```

Варианты ответов перемешиваются при каждом показе (`prepare()` в
`src/lib/questions.ts`); задания типа `true_false` сохраняют порядок
«Верно / Неверно».

### graph_spec

Графики рисует `src/components/GraphRenderer.tsx` — чистый SVG с сеткой, осями,
стрелками, засечками и обрезкой кривой по границе области. Поддерживаются:

| `type` | `params` | Функция |
| --- | --- | --- |
| `linear` | `k`, `b` | $y = kx + b$ |
| `parabola` | `a`, `b`, `c` | $y = ax^2 + bx + c$ |
| `hyperbola` | `k`, `shiftX`, `shiftY` | $y = \frac{k}{x - shiftX} + shiftY$ (асимптоты рисуются пунктиром) |
| `trig` | `fn`, `A`, `k`, `phase`, `shift` | $y = A\,fn(kx + phase) + shift$, засечки по оси $x$ в долях $\pi$ |

У любого типа можно задать окно просмотра: `params.view = { xMin, xMax, yMin, yMax }`.

### Добавить задание

Допишите объект в `src/data/ct_questions.json` — ничего больше менять не нужно:
раунды, формульник и статистика собираются из этого файла. Требования: уникальный
`id` (`[a-z0-9_]`), 4 варианта (или 2 для `true_false`), непустые `rule` и
`common_trap`, парное число символов `$`.

## Структура

```
src/
  data/ct_questions.json      банк заданий
  types.ts                    типы, метаданные категорий
  lib/
    questions.ts              загрузка, перемешивание, сборка раунда, очки
    storage.ts                прогресс в localStorage, банк ошибок
  components/
    GameScreen.tsx            таймер, комбо, очередь ошибок, переход в разбор
    QuestionCard.tsx          карточка вопроса и варианты
    ExplanationModal.tsx      оверлей «Попались в ловушку»
    GraphRenderer.tsx         SVG-координатная плоскость и кривые
    FormulaBook.tsx           формульник с поиском и статусами
    HomeScreen.tsx            выбор тем и длины раунда
    ResultScreen.tsx          итоги раунда
    Tex.tsx                   KaTeX + минимальный Markdown
```

## iOS-сборка (IPA для Sideloadly)

Приложение упаковано в нативную оболочку через **Capacitor 8**: тот же веб-бандл
работает внутри `WKWebView`, все ассеты (включая шрифты KaTeX) лежат локально,
поэтому приложение полностью работает офлайн.

IPA собирается на GitHub Actions — macOS-раннер, **без сертификатов Apple**.
Подпись вы делаете сами в Sideloadly своим Apple ID.

### Как получить IPA

1. Залейте репозиторий на GitHub.
2. Вкладка **Actions** → workflow **Build iOS IPA (unsigned)** → **Run workflow**.
   Сборка занимает примерно 8–12 минут.
3. Скачайте артефакт `CTMathBlitz-<версия>-build<N>-unsigned.ipa` внизу страницы
   прогона. GitHub отдаёт артефакты в zip — внутри лежит сам `.ipa`.
4. Откройте Sideloadly, перетащите `.ipa`, укажите Apple ID и нажмите **Start**.

Тег вида `v1.0.0` дополнительно создаёт GitHub Release с прикреплённым IPA.

### Что важно знать про сайдлоад

- С бесплатным Apple ID подпись живёт **7 дней**, дальше приложение нужно
  переустановить (или включить в Sideloadly автообновление через Wi-Fi).
- Бесплатный аккаунт разрешает не больше **3 сайдлоад-приложений** на устройстве
  и 10 новых App ID в неделю.
- Bundle ID по умолчанию — `by.ctmath.blitz` (задаётся в `capacitor.config.ts`).
  Sideloadly умеет менять его на лету, если понадобится.
- Первый запуск: **Настройки → Основные → VPN и управление устройством** →
  доверить своему профилю разработчика.

### Что делает workflow

`.github/workflows/ios.yml`, по шагам:

1. `npm ci` и `npm run build` — обычный веб-бандл в `dist/`.
2. `npx cap add ios` — генерирует нативный проект и запускает `pod install`.
   Папка `ios/` **не хранится в репозитории**: она целиком выводится из
   `capacitor.config.ts` и пересоздаётся на каждой сборке.
3. `node scripts/ios-assets.mjs` — рендерит `assets/icon.svg` и
   `assets/splash.svg` в `Assets.xcassets`. Скрипт перезаписывает каждый
   существующий PNG в его же размере, поэтому не ломается при смене версии
   Capacitor. Иконка пишется без альфа-канала — этого требует iOS.
4. PlistBuddy проставляет название, версию (`CFBundleShortVersionString` из
   `package.json`), номер сборки (номер прогона) и светлый статус-бар.
5. `xcodebuild archive` с `CODE_SIGNING_ALLOWED=NO`.
6. `App.app` кладётся в `Payload/` и зипуется в `.ipa`.

Проверить ассеты локально, без macOS:

```bash
npm run ios:assets -- --preview asset-preview
```

Если есть Mac, полный нативный цикл разворачивается одной командой:

```bash
npm run ios:add && npx cap open ios
```

### Известные мелочи

- `npm audit` показывает 3 moderate в `uuid` — это транзитивная зависимость
  `@capacitor/cli` (через `xcode`), инструмент сборки. В само приложение этот код
  не попадает; фикс требует даунгрейда CLI ниже версии `@capacitor/core`.
- Раннер закреплён на `macos-15`. Если GitHub выведет образ из обращения,
  поменяйте `runs-on` в workflow.
